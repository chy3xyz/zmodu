//! Barrel re-exports for module `<<MODULE_NAME>>` (zent Schema-as-Code).

pub const schema = @import("schema.zig");
pub const client = @import("client.zig");
pub const module = @import("module.zig");
